"設定
set title
set showmatch
syntax on
set tabstop=4
set smartindent

" 日本語エンコーディング設定(UTF-8)
set encoding=utf-8

" 改行コード設定(LF)
set ff=unix

" スワップファイルを作らない
set noswapfile

" バックアップファイルを作らない
set nobackup

" unファイルを作らない
set noundofile
